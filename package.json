{
  "name": "gif_manager",
  "version": "1.3.6",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "powershell -NoProfile -Command \"if (Test-Path dist) { Get-ChildItem dist -Force | Remove-Item -Recurse -Force }; Copy-Item -Recurse -Force src/* dist/\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  }
}

